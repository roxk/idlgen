# idlgen testing Sample App

## Prerequisite

1. idlgen (See [it's README](../README.md))

## Getting Started

1. Run `./scripts/build-nuget -version 0.0.0` at project root
2. Copy the generated nuget to `LocalPackages`
3. Build `SampleApp` project
